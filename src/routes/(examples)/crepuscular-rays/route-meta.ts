import type { RouteMeta } from "../../api/routes/+server.ts";

export const meta: RouteMeta = {
  status: "Completed",
  note: "A visual effect that creates light beam/crepuscular rays using CSS and SVG filters",
  tags: ["animation", "hero"],
  featured: false,
};
